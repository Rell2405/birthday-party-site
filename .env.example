# Base URL of the deployed Cloudflare Worker API.
# Leave empty to run in static demo mode (data saved only in the visitor's browser).
# Example: PUBLIC_API_BASE=https://birthday-party-api.<your-subdomain>.workers.dev
PUBLIC_API_BASE=
